{
  "name": "blockchain",
  "version": "1.0.0",
  "description": "Blockchain made in JS following the YT tutorial in README",
  "main": "blockchain.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --noImplicitReturns --noFallthroughCasesInSwitch --strict --noUnusedLocals ts_test/*.ts;",
    "start": "node ts_test"
  },
  "author": "PiErr0r",
  "license": "ISC",
  "dependencies": {
    "crypto-js": "^4.0.0",
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@types/node": "^14.14.41"
  }
}
